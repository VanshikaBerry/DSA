#include<iostream>
using namespace std;
replaceallpi(string str)
{
    string x=str.substr(0,2);
    if(x=="Pi")
    {
        "3.14"+replaceallpi
    }

}
int main()
{
    string str;
    cout<<"enter the string ";
    getline(cin,str);
    replacepi(str);
}